apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-claim
  namespace: federation
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 400Mi
